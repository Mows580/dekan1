# AGENTS.md

## Cursor Cloud specific instructions

### Overview

This is an empty Python project scaffold (`dekan1`). The repository currently contains only a `README.md` and a Python-oriented `.gitignore`. No application code, dependencies, or services exist yet.

### Environment

- **Python**: 3.12.3 (system Python at `/usr/bin/python3`)
- **Package manager**: pip (no `requirements.txt`, `pyproject.toml`, or other dependency files exist yet)
- **No virtual environment** is pre-created; create one with `python3 -m venv .venv` if needed when dependencies are added.

### Development

- No lint, test, or build commands are configured yet.
- When code is added, update this section with relevant commands.
- The `.gitignore` is configured for Python projects (covers `__pycache__`, `.venv`, `.tox`, `.pytest_cache`, etc.).

### Notes for future agents

- If a `pyproject.toml` or `requirements.txt` is added, update the VM environment script accordingly.
- No services need to be started for this repository in its current state.
